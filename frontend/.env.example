# Realtouch Invoice frontend environment template
# Copy this to frontend/.env before local builds, or add these variables inside Vercel/other frontend host.

REACT_APP_BACKEND_URL=https://api-invoice.yourdomain.com
REACT_APP_GOOGLE_CLIENT_ID=
